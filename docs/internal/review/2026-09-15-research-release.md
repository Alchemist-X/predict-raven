# 研究更新合并验收

[English](2026-09-15-research-release.en.md)

## 范围与行为

收口 `codex/forecast-answer-types` 相对 `origin/main` 的 12 个既有提交及当前工作区的 59 个源码/文档改动，不包含其他旧工作区。答案保留二元、互斥选项、数值、独立事件排名的含义；研究默认不限轮次，重大证据缺口触发回搜。未完成研究在 API 和 Raven 中返回暂定估计及明确状态，不输出最终结论。

扩展资源库区分候选发现、真实文字阅读和 PDF 页文。模型按问题选择正文重要图表，原生 MCP 传入图片后保存视觉观察；文字路线明确报告无法读图，不把图片下载或 OCR 当成已核验正文。私人正文、缓存和凭证不随本次公开代码合并。

## 验证

- 引擎与 Forecast API：30 个测试文件、381 项测试通过。
- Raven 纯逻辑：5 项测试通过。
- 引擎、Forecast API、Raven：三处类型检查通过。
- `pnpm --filter @autopoly/raven build`：production build 通过。
- 1440×1000 与 390×844：首页、三类新答案、未完成排名、二元未完成报告、三类未完成研究页、既有研究演示，共 20 个页面场景通过；另验证中文切换和报告到研究页导航。
- 浏览器记录无 console error、pageerror、错误浮层或页面横向溢出。人工查看截图后，答案单位、独立概率说明、暂定标识、证据段落与移动布局均符合预期。
- 公开树的凭证模式扫描未命中；与本机 1736 篇订阅缓存比对，改动文件没有连续 700 字符的正文拷贝。此检查针对精确复制，不声明覆盖改写内容。

使用合成 fixture 与既有演示进行页面验收；没有提交研究任务、发起付费模型请求或执行交易。原始运行证据保存在本机 `/tmp/raven-merge-research-release-20260915/`，包括测试/build 日志、截图、页面错误记录和源码 hash 清单。

## 发布边界

合并 main 会触发仓库 CI。此次不改部署配置；Raven 与 Forecast API 的宿主部署按 `deploy/raven` 的既有流程更新。代码验收不等于线上部署验收。
